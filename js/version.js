// Overwritten at deploy time by .github/workflows/deploy-pages.yml with the
// commit's date + short SHA, so the header stamp always identifies the build
// actually running on the phone. 'dev' means a local / non-deployed copy.
const BUILD = 'dev';
